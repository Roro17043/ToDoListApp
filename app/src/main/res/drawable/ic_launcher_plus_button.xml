<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android"> <!-- Background circle -->
    <item>
        <shape
            android:shape="oval">
            <solid android:color="@android:color/holo_blue_light" />
            <size
                android:width="56dp"
                android:height="56dp" />
        </shape>
    </item>
    <!-- Plus sign -->
    <item>
        <bitmap
            android:src="@android:drawable/btn_plus"
            android:gravity="center" />
    </item></selector>